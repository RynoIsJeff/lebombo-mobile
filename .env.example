# Where the Lebombo platform lives. The field app is deployed separately and
# talks to it over HTTPS, so this must be the full origin with no trailing slash.
NEXT_PUBLIC_API_BASE=https://your-lebombo-app.vercel.app
